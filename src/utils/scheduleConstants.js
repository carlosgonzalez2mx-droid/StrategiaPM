/**
 * @fileoverview Constantes compartidas para el módulo de cronogramas
 * @module utils/scheduleConstants
 */

/**
 * Milisegundos en un día (24 horas)
 * @constant {number}
 */
export const DAY = 1000 * 60 * 60 * 24;

/**
 * Altura de cada fila en el diagrama de Gantt (en píxeles)
 * @constant {number}
 */
export const ROW_H = 50;

/**
 * Padding superior para el diagrama de Gantt (en píxeles)
 * @constant {number}
 */
export const PADDING_TOP = 10;
